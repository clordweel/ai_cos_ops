# list-item-groups（MCP 优先：物料组列表）

请指导用户在指定环境用 **FAC MCP** 拉取 `Item Group` 指定字段，并保存到 `work/<env>/reference/item_groups.json`。

```bash
python scripts/fac_mcp_list_item_groups.py --env dev --limit 500
```

