# scripts 子目录约束

## 目标

脚本的职责是：**识别环境（dev/prod）、输出醒目标识、在高风险操作前做硬性护栏**。不要把业务逻辑塞进脚本里。

## 规范

- 脚本必须默认安全：不带 `--env` 不执行写操作。
- 对 `ENV=prod` 的写操作必须要求双确认。
- 输出必须包含：ENV / SITE_URL / MCP_URL（或 REST_BASE_URL）。

