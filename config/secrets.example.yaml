# 本文件是模板：可以提交进仓库。
# 请复制为 `config/secrets.local.yaml` 并填入真实值（该文件已被 .gitignore 忽略，绝不提交）。

# Frappe REST 鉴权（常见为 API Key + API Secret）
rest_api_key: "YOUR_API_KEY"
rest_api_secret: "YOUR_API_SECRET"

#（可选）FAC MCP（Frappe_Assistant_Core）需要 OAuth 2.0 Bearer access_token
# 注意：这不是 api_key:api_secret。通常需要在 LLM Connector / MCP Inspector 中走 OAuth 登录后获取。
mcp_token: ""
